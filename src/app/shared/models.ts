export interface IClient 
{
    id?: number,
    adresse? : String,
    cp? : String,
    email? : String,
    pays : String,
    raisonSociale : String,
    responsable : String,
    ville : String
}